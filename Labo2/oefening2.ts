enum kleur
{
Red, Orange, Yellow, Green, Blue, Indigo, Violet
}
let legearray = new Array(kleur);
for (let index = 0 ; index <= 6; index++)
{
    
    legearray[index] ;
}
//console.log(legearray)
//console.log(legearray.length)
while(legearray.length <= 6)
{
    console.log(legearray.pop());
    break;
}