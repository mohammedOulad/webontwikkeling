let veelvouden = new Array();

for (let i = 0; i < 70; i++)
    {  
        if (getallen[i] % 7 === 0)
    {
        console.log(getallen[i]);
        let teller = 70 +(i+1);
        veelvouden[i] = teller;  
    }
    }